diff --git a/src/Transports/MassTransit.Azure.ServiceBus.Core/AzureServiceBusTransport/ConnectionContextFactory.cs b/src/Transports/MassTransit.Azure.ServiceBus.Core/AzureServiceBusTransport/ConnectionContextFactory.cs
index d618e9819..02b5f5a79 100644
--- a/src/Transports/MassTransit.Azure.ServiceBus.Core/AzureServiceBusTransport/ConnectionContextFactory.cs
+++ b/src/Transports/MassTransit.Azure.ServiceBus.Core/AzureServiceBusTransport/ConnectionContextFactory.cs
@@ -4,6 +4,7 @@ namespace MassTransit.AzureServiceBusTransport
     using System.Threading;
     using System.Threading.Tasks;
     using Agents;
+    using Azure;
     using Azure.Identity;
     using Azure.Messaging.ServiceBus;
     using Azure.Messaging.ServiceBus.Administration;
@@ -45,7 +46,8 @@ async Task<ConnectionContext> CreateSharedConnection(Task<ConnectionContext> con
 
         async Task<ConnectionContext> CreateConnection(ISupervisor supervisor)
         {
-            var endpoint = new UriBuilder(_hostConfiguration.HostAddress) { Path = "" }.Uri.Host;
+            var hostUri = new UriBuilder(_hostConfiguration.HostAddress) { Path = "" }.Uri;
+            var endpoint = hostUri.Host;
 
             if (supervisor.Stopping.IsCancellationRequested)
                 throw new ServiceBusConnectionException($"The connection is stopping and cannot be used: {endpoint}");
@@ -74,9 +76,17 @@ async Task<ConnectionContext> CreateConnection(ISupervisor supervisor)
                     MaxRetries = settings.RetryLimit,
                     Mode = Azure.Core.RetryMode.Exponential,
                     MaxDelay = settings.RetryMaxBackoff
-                }
+                },
             };
 
+            // When the host URI contains a non-default port (emulator scenario),
+            // construct an admin client from a connection string with
+            // UseDevelopmentEmulator=true so the SDK uses plain HTTP on that
+            // port rather than defaulting to HTTPS:443. The key is read from
+            // the MT_ASB_KEYVALUE env var since AzureNamedKeyCredential doesn't
+            // expose its key publicly.
+            var emulatorConnectionString = BuildEmulatorConnectionString(hostUri, settings.NamedKeyCredential);
+
             if (settings.TokenCredential != null)
             {
                 client ??= new ServiceBusClient(endpoint, settings.TokenCredential, clientOptions);
@@ -84,8 +94,16 @@ async Task<ConnectionContext> CreateConnection(ISupervisor supervisor)
             }
             else if (settings.NamedKeyCredential != null)
             {
-                client ??= new ServiceBusClient(endpoint, settings.NamedKeyCredential, clientOptions);
-                managementClient ??= new ServiceBusAdministrationClient(endpoint, settings.NamedKeyCredential, managementOptions);
+                if (emulatorConnectionString != null)
+                {
+                    client ??= new ServiceBusClient(emulatorConnectionString, clientOptions);
+                    managementClient ??= new ServiceBusAdministrationClient(emulatorConnectionString, managementOptions);
+                }
+                else
+                {
+                    client ??= new ServiceBusClient(endpoint, settings.NamedKeyCredential, clientOptions);
+                    managementClient ??= new ServiceBusAdministrationClient(endpoint, settings.NamedKeyCredential, managementOptions);
+                }
             }
             else if (settings.SasCredential != null)
             {
@@ -118,5 +136,35 @@ static bool HasSharedAccess(string connectionString)
             return !string.IsNullOrEmpty(connectionStringProperties.SharedAccessKeyName)
                 && !string.IsNullOrEmpty(connectionStringProperties.SharedAccessKey) || !string.IsNullOrEmpty(connectionStringProperties.SharedAccessSignature);
         }
+
+        /// <summary>
+        /// When the host URI contains a non-default port (emulator scenario) and a
+        /// NamedKeyCredential is configured, build a connection string with
+        /// <c>UseDevelopmentEmulator=true</c> so that <see cref="ServiceBusAdministrationClient"/>
+        /// uses plain HTTP on the configured port instead of defaulting to HTTPS:443.
+        ///
+        /// <para>
+        /// The key is read from the <c>MT_ASB_KEYVALUE</c> environment variable because
+        /// <see cref="AzureNamedKeyCredential"/> does not expose its key publicly. Returns
+        /// <c>null</c> when the default port is used (i.e. real Azure Service Bus) or when
+        /// the key env var is not set.
+        /// </para>
+        /// </summary>
+        static string BuildEmulatorConnectionString(Uri hostUri, AzureNamedKeyCredential credential)
+        {
+            if (hostUri.Port <= 0 || hostUri.IsDefaultPort)
+                return null;
+
+            if (credential == null)
+                return null;
+
+            var key = Environment.GetEnvironmentVariable("MT_ASB_KEYVALUE");
+            if (string.IsNullOrEmpty(key))
+                return null;
+
+            return $"Endpoint=sb://{hostUri.Host}:{hostUri.Port};"
+                 + $"SharedAccessKeyName={credential.Name};"
+                 + $"SharedAccessKey={key};UseDevelopmentEmulator=true";
+        }
     }
 }
diff --git a/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusEndpointAddress.cs b/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusEndpointAddress.cs
index 91bec7e0a..226ddc398 100644
--- a/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusEndpointAddress.cs
+++ b/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusEndpointAddress.cs
@@ -28,6 +28,7 @@ public enum AddressType
         public readonly string Scheme;
         public readonly string Host;
         public readonly string Scope;
+        public readonly int Port;
 
         public readonly string Name;
         public readonly TimeSpan? AutoDelete;
@@ -38,6 +39,7 @@ public ServiceBusEndpointAddress(Uri hostAddress, Uri address, AddressType type
             Scheme = default;
             Host = default;
             Scope = default;
+            Port = -1;
 
             AutoDelete = default;
             Type = type;
@@ -48,18 +50,19 @@ public ServiceBusEndpointAddress(Uri hostAddress, Uri address, AddressType type
                 case "sb":
                     Scheme = address.Scheme;
                     Host = address.Host;
+                    Port = address.IsDefaultPort ? -1 : address.Port;
 
                     address.ParseHostPathAndEntityName(out Scope, out Name);
                     break;
 
                 case "queue":
-                    ParseLeft(hostAddress, out Scheme, out Host, out Scope);
+                    ParseLeft(hostAddress, out Scheme, out Host, out Scope, out Port);
 
                     Name = address.AbsolutePath;
                     break;
 
                 case "topic":
-                    ParseLeft(new Uri(hostAddress.GetLeftPart(UriPartial.Authority)), out Scheme, out Host, out Scope);
+                    ParseLeft(new Uri(hostAddress.GetLeftPart(UriPartial.Authority)), out Scheme, out Host, out Scope, out Port);
 
                     Name = address.AbsolutePath;
                     Type = AddressType.Topic;
@@ -86,7 +89,7 @@ public ServiceBusEndpointAddress(Uri hostAddress, Uri address, AddressType type
 
         public ServiceBusEndpointAddress(Uri hostAddress, string name, TimeSpan? autoDelete = default, AddressType type = AddressType.Queue)
         {
-            ParseLeft(hostAddress, out Scheme, out Host, out Scope);
+            ParseLeft(hostAddress, out Scheme, out Host, out Scope, out Port);
 
             Name = name;
 
@@ -96,12 +99,13 @@ public ServiceBusEndpointAddress(Uri hostAddress, string name, TimeSpan? autoDel
 
         public string Path => Scope == "/" ? Name : $"{Scope}/{Name}";
 
-        static void ParseLeft(Uri address, out string scheme, out string host, out string scope)
+        static void ParseLeft(Uri address, out string scheme, out string host, out string scope, out int port)
         {
             var hostAddress = new ServiceBusHostAddress(address);
             scheme = hostAddress.Scheme;
             host = hostAddress.Host;
             scope = hostAddress.Scope;
+            port = hostAddress.Port;
         }
 
         public static implicit operator Uri(in ServiceBusEndpointAddress address)
@@ -115,6 +119,9 @@ public static implicit operator Uri(in ServiceBusEndpointAddress address)
                     : $"/{address.Scope}/{address.Name}"
             };
 
+            if (address.Port > 0)
+                builder.Port = address.Port;
+
             builder.Query += string.Join("&", address.GetQueryStringOptions());
 
             return builder.Uri;
diff --git a/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusHostAddress.cs b/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusHostAddress.cs
index 2584e0d32..83451a3a7 100644
--- a/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusHostAddress.cs
+++ b/src/Transports/MassTransit.Azure.ServiceBus.Core/ServiceBusHostAddress.cs
@@ -11,12 +11,14 @@ public readonly struct ServiceBusHostAddress
         public readonly string Scheme;
         public readonly string Host;
         public readonly string Scope;
+        public readonly int Port;
 
         public ServiceBusHostAddress(Uri address)
         {
             Scheme = default;
             Host = default;
             Scope = default;
+            Port = -1;
 
             var scheme = address.Scheme.ToLowerInvariant();
             switch (scheme)
@@ -24,6 +26,7 @@ public ServiceBusHostAddress(Uri address)
                 case "sb":
                     Scheme = address.Scheme;
                     Host = address.Host;
+                    Port = address.IsDefaultPort ? -1 : address.Port;
 
                     ParseLeft(address, out Scheme, out Host, out Scope);
                     break;
@@ -52,6 +55,9 @@ public static implicit operator Uri(in ServiceBusHostAddress address)
                     : $"/{Uri.EscapeDataString(address.Scope)}"
             };
 
+            if (address.Port > 0)
+                builder.Port = address.Port;
+
             return builder.Uri;
         }
 
diff --git a/src/Transports/MassTransit.Azure.ServiceBus.Core/Testing/AzureServiceBusTestHarness.cs b/src/Transports/MassTransit.Azure.ServiceBus.Core/Testing/AzureServiceBusTestHarness.cs
index 9e1a099c6..b2d5744f1 100644
--- a/src/Transports/MassTransit.Azure.ServiceBus.Core/Testing/AzureServiceBusTestHarness.cs
+++ b/src/Transports/MassTransit.Azure.ServiceBus.Core/Testing/AzureServiceBusTestHarness.cs
@@ -78,9 +78,27 @@ public override async Task Clean()
 
         ServiceBusAdministrationClient CreateManagementClient()
         {
-            var endpoint = new UriBuilder(HostAddress) { Path = "" }.Uri.ToString();
+            // HostAddress is sb://host[:port]/.
+            var hostUri = new UriBuilder(HostAddress) { Path = "" }.Uri;
+
+            // When a non-default port is used (emulator), build a connection string
+            // with UseDevelopmentEmulator=true so the Azure SDK uses plain HTTP on
+            // that port instead of defaulting to HTTPS:443. AzureNamedKeyCredential
+            // doesn't expose its key publicly, so read from MT_ASB_KEYVALUE env var.
+            if (hostUri.Port > 0 && !hostUri.IsDefaultPort)
+            {
+                var key = Environment.GetEnvironmentVariable("MT_ASB_KEYVALUE");
+                if (!string.IsNullOrEmpty(key))
+                {
+                    var connectionString =
+                        $"Endpoint=sb://{hostUri.Host}:{hostUri.Port};"
+                      + $"SharedAccessKeyName={NamedKeyCredential.Name};"
+                      + $"SharedAccessKey={key};UseDevelopmentEmulator=true";
+                    return new ServiceBusAdministrationClient(connectionString);
+                }
+            }
 
-            return new ServiceBusAdministrationClient(endpoint, NamedKeyCredential);
+            return new ServiceBusAdministrationClient(hostUri.ToString(), NamedKeyCredential);
         }
 
         protected override async Task<IBusControl> CreateBus()
diff --git a/tests/MassTransit.Azure.ServiceBus.Core.Tests/AzureServiceBusEndpointUriCreator.cs b/tests/MassTransit.Azure.ServiceBus.Core.Tests/AzureServiceBusEndpointUriCreator.cs
index cef4cd3f9..2484d623a 100644
--- a/tests/MassTransit.Azure.ServiceBus.Core.Tests/AzureServiceBusEndpointUriCreator.cs
+++ b/tests/MassTransit.Azure.ServiceBus.Core.Tests/AzureServiceBusEndpointUriCreator.cs
@@ -7,6 +7,15 @@ public static class AzureServiceBusEndpointUriCreator
     {
         public static Uri Create(string serviceBusNamespace, string entityPath = null, string azureEndPoint = "servicebus.windows.net")
         {
+            // Allow overriding the entire sb:// endpoint via environment variable for emulator use.
+            var emulatorEndpoint = Environment.GetEnvironmentVariable("MT_ASB_ENDPOINT");
+            if (emulatorEndpoint != null)
+            {
+                var baseUri = emulatorEndpoint.TrimEnd('/');
+                var fullUri = entityPath != null ? $"{baseUri}/{entityPath}" : $"{baseUri}/";
+                return new Uri(fullUri);
+            }
+
             var endpoint = $"sb://{serviceBusNamespace}.{azureEndPoint}/{entityPath}";
 
             return new Uri(endpoint);
diff --git a/tests/MassTransit.Azure.ServiceBus.Core.Tests/Configuration.cs b/tests/MassTransit.Azure.ServiceBus.Core.Tests/Configuration.cs
index cc0d52e31..fbca543f3 100644
--- a/tests/MassTransit.Azure.ServiceBus.Core.Tests/Configuration.cs
+++ b/tests/MassTransit.Azure.ServiceBus.Core.Tests/Configuration.cs
@@ -37,6 +37,11 @@ static class Configuration
 
         public static ServiceBusAdministrationClient GetManagementClient()
         {
+            // When an emulator endpoint is configured, build the management client from
+            // a connection string so that the correct host and port are used.
+            if (EmulatorEndpoint != null)
+                return new ServiceBusAdministrationClient(CreateConnectionString(ServiceNamespace, KeyName, SharedAccessKey));
+
             var hostAddress = AzureServiceBusEndpointUriCreator.Create(ServiceNamespace);
             var accountSettings = new TestAzureServiceBusAccountSettings();
 
@@ -56,9 +61,19 @@ public static ServiceBusAdministrationClient GetManagementClient()
 
         public static string CreateConnectionString(string? ns, string? keyName, string? key)
         {
+            if (EmulatorEndpoint != null)
+                return $"Endpoint={EmulatorEndpoint};SharedAccessKeyName={keyName};SharedAccessKey={key};UseDevelopmentEmulator=true";
             return $"Endpoint=sb://{ns}.servicebus.windows.net/;SharedAccessKeyName={keyName};SharedAccessKey={key}";
         }
 
+        /// <summary>
+        /// Optional custom sb:// endpoint for emulator testing, e.g. "sb://localhost:5672".
+        /// When set, connection strings and URI creation use this endpoint instead of
+        /// the default {namespace}.servicebus.windows.net pattern.
+        /// </summary>
+        public static string? EmulatorEndpoint =>
+            Environment.GetEnvironmentVariable("MT_ASB_ENDPOINT");
+
         public static void UsingTestAzureServiceBus(this IBusRegistrationConfigurator configurator,
             Action<IBusRegistrationContext, IServiceBusBusFactoryConfigurator>? configure = null, bool configureEndpoints = true)
         {
